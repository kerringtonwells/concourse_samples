---

platform: linux

inputs:
- name: mounted-empty-dir
- name: dir-created-by-input-but-wont-be-passed-as-output-to-later-tasks

run: 
  path: sh
  args:
  - -exc
  - |
    ls -ltrha
    echo ""
    echo "Should be a folder called mounted_folder which should be visible because we called the input"
    ls -ltrha mounted-empty-dir/*
